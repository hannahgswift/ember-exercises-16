{{log data.firstName}}

<button class="new-contact__btn" {{action "toggleForm"}}>Create New Contact</button>

{{#if isShowingForm}}
<form class="new-contact__form" {{action "addContact" on="submit"}}>
  <p>
    <label>First Name</label>
    {{input type="text" value=firstName}}
  </p>

  <p>
    <label>Last Name</label>
    {{input type="text" value=lastName}}
  </p>

  <p>
    <label>Address</label>
    {{input type="text" value=address}}
  </p>

  <p>
    <label>Phone Number</label>
    {{input type="text" value=phoneNumber}}
  </p>

  <button>Submit</button>
</form>
{{/if}}

{{#each model as |dog|}}
  <div class="contacts-list">
    <h4 class="first-name">{{dog.firstName}} {{dog.lastName}}</h4>
    <p class="address">{{dog.address}}</p>
    <p class="phone-number">{{dog.phoneNumber}}</p>
    <button {{action "deleteContact" on="click"}}>Delete</button>
  </div>
{{/each}}

{{outlet}}
